import React from 'react';
import App from '../App';
import { useHistory } from 'react-router-dom';

const User = (props) => {
    const history = useHistory();
    return (
        <div>
            
        </div>
    )
}

export default User;
